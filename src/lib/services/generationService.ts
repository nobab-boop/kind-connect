/**
 * Generation service — placeholder. Will orchestrate prompt composition,
 * randomization, and AI routing in later phases.
 */

export const generationService = {
  async run(_expertSlug: string, _inputs: Record<string, unknown>) {
    throw new Error("generationService.run not implemented");
  },
};
