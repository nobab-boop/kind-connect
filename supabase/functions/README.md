# Edge Functions

Placeholder directory. No functions exist yet — CreatorVault ships business
logic through TanStack `createServerFn` and server routes in the app runtime.
Add Supabase Edge Functions here only when a workload must run on Supabase's
Deno runtime (e.g. long-running webhooks, database triggers).
